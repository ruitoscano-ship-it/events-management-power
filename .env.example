# Supabase (opcional). Sem estas variáveis a app usa localStorage (demo).
# Dashboard Supabase → Project Settings → API
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_or_publishable_key

# Para npm run db:migrate (SQL scripts) — Database → Connection string → URI
# DATABASE_URL=postgresql://postgres.[ref]:[PASSWORD]@aws-0-eu-west-1.pooler.supabase.com:5432/postgres
# ou só a password:
# SUPABASE_DB_PASSWORD=your_database_password
